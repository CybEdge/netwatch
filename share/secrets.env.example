# Copy to ~/.config/netwatch/secrets.env (chmod 600)
# Or run: netwatch --configure

ROUTER_USER=
ROUTER_PASSWORD=

# Optional — email alerts (see config.yaml notifications.email)
NETWATCH_SMTP_USER=
NETWATCH_SMTP_PASS=
